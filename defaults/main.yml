---
# Configure role default behaviour
config_mode: false
setup: false
upload_data: false
upload_policy: false
install_method: "download"

opa:
  url: https://github.com/open-policy-agent/opa/releases/download
  architecture: opa_linux_amd64
  build_path: /opt/opa
  version: 0.23.0
  install_dir: /usr/local/bin